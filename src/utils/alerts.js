export const successAlert = (messageApi, content) => {
    messageApi.success(content);
};
export const errorAlert = (messageApi, content) => {
    messageApi.error(content);
};
export const warningAlert = (messageApi, content) => {
    messageApi.warning(content);
};
